# UX-Politur (Stufe 5a) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reines Frontend-Styling-Polish: klickbares Logo, aufgeräumte Sidebar, zentrierte Account-Seite, verspielte Empty States, konsistente Loading-Skeletons, Mobile-Fixes im Magic-Link-Flow, klare Dashboard-Priorisierung.

**Architecture:** Drei neue wiederverwendbare Komponenten (`EmptyState`, `Skeleton`, `Card`) nach dem Vorbild des bestehenden `PageHeader`. Keine Backend-Änderungen, keine neuen Endpunkte, keine Datenmodell-Änderungen.

**Tech Stack:** React, TypeScript, Tailwind CSS (bestehendes Setup, keine neuen Abhängigkeiten).

---

### Task 1: `EmptyState`-Komponente

**Files:**
- Create: `frontend/src/components/EmptyState.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
// frontend/src/components/EmptyState.tsx
import type { ReactNode } from "react";

interface EmptyStateProps {
  icon: string; // Emoji, z.B. "🚀"
  title: string;
  description: string;
  action?: ReactNode; // z.B. ein <Link>/<button> - bewusst als ReactNode statt
  // fixer Label/onClick-Props, damit sowohl <Link to="..."> (Navigation)
  // als auch <button onClick={...}> (z.B. "Formular öffnen") passen.
}

/** Verspielter Platzhalter für leere Listen/Zustände - siehe Design-Spec
 * "UX-Politur" Abschnitt 3. Ersetzt die bisherigen ad-hoc
 * "Noch nichts hier"-Absätze pro Seite durch eine einheitliche Optik. */
export function EmptyState({ icon, title, description, action }: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center gap-2 rounded-xl border border-dashed border-white/10 bg-surface/40 px-6 py-12 text-center">
      <span className="text-4xl">{icon}</span>
      <p className="text-base font-semibold text-white">{title}</p>
      <p className="max-w-sm text-sm text-slate-400">{description}</p>
      {action && <div className="mt-2">{action}</div>}
    </div>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler (Komponente wird noch nirgends importiert, reiner Additiv-Check)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/EmptyState.tsx
git commit -m "feat: add reusable EmptyState component"
```

---

### Task 2: `Skeleton`-Komponente

**Files:**
- Create: `frontend/src/components/Skeleton.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
// frontend/src/components/Skeleton.tsx

/** Schmaler pulsierender Balken - für einzelne Textzeilen-Platzhalter. */
export function SkeletonLine({ className = "" }: { className?: string }) {
  return <div className={`animate-pulse rounded bg-white/10 ${className}`} />;
}

/** Karten-förmiger Platzhalter (Bild oben, Zeilen darunter) - für
 * Listen-/Grid-Ansichten wie Dashboard-Klientenkarten oder Timeline-Fotos,
 * siehe Design-Spec "UX-Politur" Abschnitt 4. */
export function SkeletonCard() {
  return (
    <div className="animate-pulse overflow-hidden rounded-xl border border-white/5 bg-surface p-4">
      <div className="mb-3 h-4 w-2/3 rounded bg-white/10" />
      <div className="mb-2 h-3 w-1/2 rounded bg-white/5" />
      <div className="h-3 w-1/3 rounded bg-white/5" />
    </div>
  );
}

/** Mehrere SkeletonCards im Grid, deckt einen typischen Lade-Zustand ab
 * (z.B. Dashboard-Klientenliste beim ersten Laden). */
export function SkeletonGrid({ count = 6 }: { count?: number }) {
  return (
    <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">
      {Array.from({ length: count }).map((_, i) => (
        <SkeletonCard key={i} />
      ))}
    </div>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Skeleton.tsx
git commit -m "feat: add reusable Skeleton loading components"
```

---

### Task 3: `Card`-Komponente

**Files:**
- Create: `frontend/src/components/Card.tsx`

- [ ] **Step 1: Komponente schreiben**

```tsx
// frontend/src/components/Card.tsx
import type { ReactNode } from "react";

interface CardProps {
  title?: string;
  description?: string;
  children: ReactNode;
  className?: string;
  danger?: boolean; // roter Rahmen statt Standard - für die Danger Zone
}

/** Einheitlicher Abschnitts-Container mit optionaler Überschrift/
 * Beschreibung - siehe Design-Spec "UX-Politur" Abschnitt 2. Ersetzt die
 * bisher pro Sektion wiederholten `rounded-xl border ... bg-surface p-4`
 * + manuelle <h2>-Blöcke in Account.tsx. */
export function Card({ title, description, children, className = "", danger = false }: CardProps) {
  return (
    <div
      className={`rounded-xl border p-4 ${
        danger ? "border-red-900/40 bg-surface" : "border-white/5 bg-surface"
      } ${className}`}
    >
      {title && <h2 className="mb-1 text-lg font-semibold text-white">{title}</h2>}
      {description && <p className="mb-4 text-sm text-slate-400">{description}</p>}
      {children}
    </div>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Card.tsx
git commit -m "feat: add reusable Card component"
```

---

### Task 4: Header-Logo klickbar + Sidebar-Toggle neu positioniert

**Files:**
- Modify: `frontend/src/components/AppShell.tsx`
- Modify: `frontend/src/components/ClientShell.tsx`

- [ ] **Step 1: Logo im Header verlinken**

In `frontend/src/components/AppShell.tsx`: `Link` zum bestehenden `react-router-dom`-Import hinzufügen (steht schon `NavLink, Outlet, useLocation, useNavigate` da, `Link` ergänzen):

```tsx
import { Link, NavLink, Outlet, useLocation, useNavigate } from "react-router-dom";
```

Den Logo-Span ersetzen:

```tsx
            <span className="text-sm font-semibold tracking-wide text-white">
              BodyComp <span className="text-accent">Tracker</span>
            </span>
```

durch:

```tsx
            <Link
              to={user?.account_type === "coach" ? "/dashboard" : "/"}
              className="text-sm font-semibold tracking-wide text-white transition-colors hover:text-accent"
            >
              BodyComp <span className="text-accent">Tracker</span>
            </Link>
```

- [ ] **Step 2: Sidebar-Toggle in ClientShell.tsx neu positionieren + Container-Hintergrund**

In `frontend/src/components/ClientShell.tsx`, den Desktop-`<nav>`-Block ersetzen. Aktuell:

```tsx
      {/* Desktop: feste Sidebar, einklappbar */}
      <nav
        className={`hidden shrink-0 flex-col gap-1 sm:flex ${
          desktopCollapsed ? "w-12" : "w-48"
        } transition-all`}
      >
        <button
          onClick={() => setDesktopCollapsed((c) => !c)}
          className="mb-2 self-end rounded-lg p-2 text-slate-500 hover:bg-white/5 hover:text-white"
          aria-label={desktopCollapsed ? "Navigation ausklappen" : "Navigation einklappen"}
        >
          {desktopCollapsed ? "»" : "«"}
        </button>
        {NAV_ITEMS.map((item) => (
          <NavLink
            key={item.to}
            to={item.to}
            end={item.to === "timeline"}
            aria-disabled={item.to === activeNavTo}
            onClick={(e) => item.to === activeNavTo && e.preventDefault()}
            className={navLinkClass}
            title={desktopCollapsed ? item.label : undefined}
          >
            <span>{item.icon}</span>
            {!desktopCollapsed && item.label}
          </NavLink>
        ))}
      </nav>
```

Ersetzen durch (Toggle wandert ans Ende, eigener Block mit Trennstrich; die ganze Nav bekommt einen dezenten Hintergrund-Container statt frei im Whitespace zu stehen):

```tsx
      {/* Desktop: feste Sidebar, einklappbar - Container fasst die Punkte
          visuell zusammen (siehe Design-Spec "UX-Politur" Abschnitt 1),
          Toggle steht unten, abgesetzt durch einen Trennstrich, statt
          isoliert oben zu "hängen". */}
      <nav
        className={`hidden shrink-0 flex-col gap-1 rounded-xl bg-surface/40 p-2 sm:flex ${
          desktopCollapsed ? "w-14" : "w-48"
        } transition-all`}
      >
        {NAV_ITEMS.map((item) => (
          <NavLink
            key={item.to}
            to={item.to}
            end={item.to === "timeline"}
            aria-disabled={item.to === activeNavTo}
            onClick={(e) => item.to === activeNavTo && e.preventDefault()}
            className={navLinkClass}
            title={desktopCollapsed ? item.label : undefined}
          >
            <span>{item.icon}</span>
            {!desktopCollapsed && item.label}
          </NavLink>
        ))}
        <div className="mt-1 border-t border-white/10 pt-1">
          <button
            onClick={() => setDesktopCollapsed((c) => !c)}
            className="flex w-full items-center justify-center rounded-lg p-2 text-slate-500 hover:bg-white/5 hover:text-white"
            aria-label={desktopCollapsed ? "Navigation ausklappen" : "Navigation einklappen"}
          >
            {desktopCollapsed ? "»" : "«"}
          </button>
        </div>
      </nav>
```

- [ ] **Step 3: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 4: Manuell im Dev-Server prüfen**

Run: `npm run dev` (falls nicht schon läuft), Login, prüfen: Logo im Header ist klickbar und führt zum Dashboard/Timeline-Redirect; Sidebar-Toggle sitzt unten mit Trennstrich, Sidebar hat sichtbaren Hintergrund-Container.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/AppShell.tsx frontend/src/components/ClientShell.tsx
git commit -m "feat: make header logo clickable, reposition sidebar toggle"
```

---

### Task 5: Account-Seite zentrieren + Card-Komponente nutzen

**Files:**
- Modify: `frontend/src/pages/Account.tsx`

- [ ] **Step 1: Import ergänzen**

Nach der bestehenden `import PageHeader from "../components/PageHeader";`-Zeile ergänzen:

```tsx
import { Card } from "../components/Card";
```

- [ ] **Step 2: Äußeren Container zentrieren**

`export default function Account()` — den Root-`<div>` ändern von:

```tsx
    <div className="max-w-xl space-y-6">
```

zu:

```tsx
    <div className="mx-auto max-w-2xl space-y-6">
```

- [ ] **Step 3: `GeminiKeySettings`, `DisplaySettingsSection`, `DangerZoneSection` auf `Card` umstellen**

In `DisplaySettingsSection` den äußeren `<div className="rounded-xl border border-white/5 bg-surface p-4">` und die manuelle `<h2>`/`<p>`-Beschreibung ersetzen durch `Card` mit `title`/`description`-Props; den restlichen Inhalt (das `<div className="grid gap-4 ...">` bis zum schließenden Button) unverändert als `children` beibehalten. Konkret wird aus:

```tsx
  return (
    <div className="rounded-xl border border-white/5 bg-surface p-4">
      <h2 className="mb-1 text-lg font-semibold text-white">Anzeige-Einstellungen</h2>
      <p className="mb-4 text-sm text-slate-400">
        Steuert, wie die Timeline gerendert wird - v.a. bei vielen Fotos/Tagen relevant für die
        Performance.
      </p>

      <div className="grid gap-4 sm:grid-cols-2">
```

wird:

```tsx
  return (
    <Card
      title="Anzeige-Einstellungen"
      description="Steuert, wie die Timeline gerendert wird - v.a. bei vielen Fotos/Tagen relevant für die Performance."
    >
      <div className="grid gap-4 sm:grid-cols-2">
```

und das schließende `</div>` am Ende der Funktion wird zu `</Card>`. Genauso für `GeminiKeySettings` (Titel "KI-Einstellungen", Description-Absatz mit dem Link bleibt als `description`-Prop — Achtung: `description` ist hier ein `string`-Prop, der bestehende Absatz enthält einen `<a>`-Link, daher **hier stattdessen** die Beschreibung als erstes Kind-Element im JSX belassen statt als Prop, nur den äußeren `<div>`/`<h2>` durch `<Card title="KI-Einstellungen">`/`</Card>` ersetzen) und für `DangerZoneSection` (Titel "Konto-Verwaltung", `danger` Prop setzen, kein description-Text vorhanden):

```tsx
// GeminiKeySettings - Beschreibung bleibt im JSX (enthält einen Link, kein reiner string):
  return (
    <Card title="KI-Einstellungen">
      <p className="mb-4 text-sm text-slate-400">
        Für die KI-Judge-Analyse in Compare wird ein kostenloser Gemini-API-Key benötigt.
        Kostenlos erstellen unter{" "}
        <a
          href="https://aistudio.google.com/apikey"
          target="_blank"
          rel="noreferrer"
          className="text-accent underline"
        >
          aistudio.google.com/apikey
        </a>
        .
      </p>
      {/* ... restlicher bestehender Inhalt unverändert ... */}
    </Card>
  );
```

```tsx
// DangerZoneSection:
  return (
    <Card title="Konto-Verwaltung" danger>
      <div className="mt-3 flex flex-col gap-3">
        {/* ... restlicher bestehender Inhalt unverändert ... */}
      </div>
    </Card>
  );
```

- [ ] **Step 4: Account-Typ-Umschaltung-Block in `Account()` auf `Card` umstellen**

Im `export default function Account()`-JSX, den Block:

```tsx
      {user?.account_type === "single" && (
        <div className="rounded-xl border border-white/5 bg-surface p-4">
          <h2 className="mb-1 text-lg font-semibold text-white">Kontotyp</h2>
          <p className="mb-4 text-sm text-slate-400">
            Du trackst aktuell nur dich selbst. Wenn du auch andere Kunden betreust, kannst du
            hier ein Dashboard mit mehreren Kundenprofilen freischalten.
          </p>
          <button
```

ersetzen durch:

```tsx
      {user?.account_type === "single" && (
        <Card
          title="Kontotyp"
          description="Du trackst aktuell nur dich selbst. Wenn du auch andere Kunden betreust, kannst du hier ein Dashboard mit mehreren Kundenprofilen freischalten."
        >
          <button
```

und das schließende `</div>` dieses Blocks zu `</Card>`.

- [ ] **Step 5: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 6: Manuell prüfen**

Account-Seite im Browser öffnen, prüfen: Inhalt ist zentriert (nicht mehr linksbündig über volle Breite), alle Abschnitte (Billing, Kontotyp, KI-Einstellungen, Anzeige, Konto-Verwaltung) sehen einheitlich aus.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/pages/Account.tsx
git commit -m "feat: center account page layout, use shared Card component"
```

---

### Task 6: Empty States einbauen (Dashboard, Timeline, Unprocessed, ClientCheckins)

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`
- Modify: `frontend/src/pages/Timeline.tsx`
- Modify: `frontend/src/pages/Unprocessed.tsx`
- Modify: `frontend/src/pages/ClientCheckins.tsx`

- [ ] **Step 1: Dashboard.tsx**

Import ergänzen: `import { EmptyState } from "../components/EmptyState";`

Ersetzen:

```tsx
      {!clientsQuery.isLoading && clients.length === 0 && (
        <div className="rounded-xl border border-dashed border-white/10 p-8 text-center text-slate-500">
          Noch keine Kunden — leg deinen ersten an.
        </div>
      )}
```

durch:

```tsx
      {!clientsQuery.isLoading && clients.length === 0 && (
        <EmptyState
          icon="🚀"
          title="Noch kein Klient an Bord"
          description="Leg deinen ersten Klienten an und starte den Check-in-Flow."
          action={
            <button
              onClick={() => setShowForm(true)}
              className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-slate-900 hover:opacity-90"
            >
              Ersten Klienten anlegen
            </button>
          }
        />
      )}
```

(Der "Keine Kunden gefunden"-Zustand bei aktivem Suchfilter bleibt unverändert als einfacher Hinweistext — das ist kein "leerer" Zustand im eigentlichen Sinn, sondern ein Filter-Ergebnis.)

- [ ] **Step 2: Timeline.tsx**

Import ergänzen: `import { EmptyState } from "../components/EmptyState";`

Ersetzen:

```tsx
  if (sortedGroups.length === 0) {
    return (
      <>
        <PageHeader title="Timeline" />
        <div className="rounded-xl border border-white/5 bg-surface p-8 text-center text-slate-400">
          Noch keine zugeordneten Fotos. Gehe zu{" "}
          <span className="text-accent">Unprocessed</span>, um Fotos einer Pose zuzuordnen.
        </div>
      </>
    );
  }
```

durch:

```tsx
  if (sortedGroups.length === 0) {
    return (
      <>
        <PageHeader title="Timeline" />
        <EmptyState
          icon="📸"
          title="Hier ist noch nichts zu sehen"
          description="Sobald der erste Check-in oder Foto-Import da ist, taucht er hier auf."
          action={
            <Link
              to={`/clients/${clientIdNum}/unprocessed`}
              className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-slate-900 hover:opacity-90"
            >
              Zu Import
            </Link>
          }
        />
      </>
    );
  }
```

`Link` aus `react-router-dom` importieren (Timeline.tsx importiert bisher `useParams` von dort — Import-Zeile entsprechend erweitern zu `import { Link, useParams } from "react-router-dom";`).

- [ ] **Step 3: Unprocessed.tsx**

Import ergänzen: `import { EmptyState } from "../components/EmptyState";`

Ersetzen:

```tsx
      {!photosQuery.isLoading && photos.length === 0 && (
        <div className="rounded-xl border border-white/5 bg-surface p-8 text-center text-slate-400">
          Keine unverarbeiteten Fotos. Klicke "Fotos hochladen", um Bilder von der eigenen
          Festplatte auszuwählen, oder lege sie direkt in{" "}
          <code className="text-accent">backend/data/photos_incoming</code> ab und klicke
          "Ordner synchronisieren".
        </div>
      )}
```

durch:

```tsx
      {!photosQuery.isLoading && photos.length === 0 && (
        <EmptyState
          icon="✨"
          title="Alles einsortiert!"
          description={'Keine unbearbeiteten Fotos mehr übrig. Klicke "Fotos hochladen" für neue Bilder.'}
          action={
            <button
              onClick={() => fileInputRef.current?.click()}
              className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-slate-900 hover:opacity-90"
            >
              📤 Fotos hochladen
            </button>
          }
        />
      )}
```

- [ ] **Step 4: ClientCheckins.tsx**

Import ergänzen: `import { EmptyState } from "../components/EmptyState";`

Ersetzen:

```tsx
      {!checkinsQuery.isLoading && checkins.length === 0 && (
        <div className="rounded-xl border border-dashed border-white/10 p-8 text-center text-slate-500">
          Noch keine Check-ins eingereicht.
        </div>
      )}
```

durch:

```tsx
      {!checkinsQuery.isLoading && checkins.length === 0 && (
        <EmptyState
          icon="📭"
          title="Noch keine Einreichungen"
          description="Teile den Check-in-Link mit deinem Klienten, damit er/sie den ersten Check-in einreichen kann."
        />
      )}
```

- [ ] **Step 5: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/Dashboard.tsx frontend/src/pages/Timeline.tsx frontend/src/pages/Unprocessed.tsx frontend/src/pages/ClientCheckins.tsx
git commit -m "feat: add playful empty states across Dashboard, Timeline, Unprocessed, Checkins"
```

---

### Task 7: Loading-Skeletons einbauen (Dashboard, Timeline)

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`
- Modify: `frontend/src/pages/Timeline.tsx`

- [ ] **Step 1: Dashboard.tsx**

Import ergänzen: `import { SkeletonGrid } from "../components/Skeleton";`

Ersetzen:

```tsx
      {clientsQuery.isLoading && <p className="text-slate-500">Lade…</p>}
```

durch:

```tsx
      {clientsQuery.isLoading && <SkeletonGrid />}
```

- [ ] **Step 2: Timeline.tsx**

Import ergänzen: `import { SkeletonCard } from "../components/Skeleton";`

Den bestehenden frühen Return-Block:

```tsx
  if (photosQuery.isLoading || dayLogsQuery.isLoading || posesQuery.isLoading || displaySettingsQuery.isLoading) {
    return (
      <>
        <PageHeader title="Timeline" />
        <p className="text-slate-500">Lade Timeline…</p>
      </>
    );
  }
```

ersetzen durch:

```tsx
  if (photosQuery.isLoading || dayLogsQuery.isLoading || posesQuery.isLoading || displaySettingsQuery.isLoading) {
    return (
      <>
        <PageHeader title="Timeline" />
        <div className="grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-5">
          {Array.from({ length: 10 }).map((_, i) => (
            <SkeletonCard key={i} />
          ))}
        </div>
      </>
    );
  }
```

- [ ] **Step 3: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/Dashboard.tsx frontend/src/pages/Timeline.tsx
git commit -m "feat: use Skeleton loading placeholders instead of plain spinners"
```

---

### Task 8: Mobile-Fixes im Magic-Link-Flow (`CheckinSubmit.tsx`)

**Files:**
- Modify: `frontend/src/pages/CheckinSubmit.tsx`

- [ ] **Step 1: Touch-Targets vergrößern, Datei-Input-Zeile mobilfreundlicher**

Das `<input type="file">`-Feld nutzt aktuell die native Browser-Darstellung, die auf schmalen Screens oft sehr klein/schwer treffbar ist. Ersetzen:

```tsx
          <label className="flex flex-col gap-1 text-sm text-slate-400">
            Fotos (optional)
            <input
              type="file"
              multiple
              accept="image/jpeg,image/png,image/heic,.heic"
              onChange={(e) => setFiles(e.target.files ? Array.from(e.target.files) : [])}
              className="text-sm text-slate-400"
            />
          </label>
```

durch (größerer Touch-Target durch Padding + Rahmen, konsistent mit den anderen Feldern im Formular):

```tsx
          <label className="flex flex-col gap-1 text-sm text-slate-400">
            Fotos (optional)
            <input
              type="file"
              multiple
              accept="image/jpeg,image/png,image/heic,.heic"
              onChange={(e) => setFiles(e.target.files ? Array.from(e.target.files) : [])}
              className="rounded-lg border border-white/10 bg-black/30 px-3 py-2 text-sm text-slate-400 file:mr-3 file:rounded-lg file:border-0 file:bg-accent file:px-3 file:py-1.5 file:text-xs file:font-medium file:text-slate-900"
            />
            {files.length > 0 && (
              <span className="text-xs text-slate-500">{files.length} Datei(en) ausgewählt</span>
            )}
          </label>
```

- [ ] **Step 2: Submit-Button als sticky Footer auf Mobile**

Der Submit-Button ist am Ende eines potenziell langen Formulars — auf Mobile muss man dafür oft weit scrollen, obwohl der Button klar der Haupt-CTA ist. Ersetzen:

```tsx
          <button
            type="submit"
            disabled={submitMutation.isPending}
            className="w-full rounded-lg bg-accent px-4 py-2 text-sm font-medium text-slate-900 hover:opacity-90 disabled:opacity-50"
          >
            {submitMutation.isPending ? "Sende…" : "Check-in einreichen"}
          </button>
```

durch (bleibt normal im Formularfluss auf Desktop/breiten Screens, wird auf schmalen Screens sticky am unteren Rand, damit er ohne Scrollen erreichbar bleibt):

```tsx
          <button
            type="submit"
            disabled={submitMutation.isPending}
            className="sticky bottom-4 w-full rounded-lg bg-accent px-4 py-3 text-sm font-medium text-slate-900 shadow-lg shadow-black/40 hover:opacity-90 disabled:opacity-50 sm:static sm:py-2 sm:shadow-none"
          >
            {submitMutation.isPending ? "Sende…" : "Check-in einreichen"}
          </button>
```

- [ ] **Step 3: Foto-Vorschau-Reihe: Scroll-Indikator/Touch-Verbesserung**

Die bestehenden `overflow-x-auto`-Foto-Reihen (bei Submissions) funktionieren auf Mobile bereits per Touch-Scroll, aber ohne visuellen Hinweis, dass mehr Fotos folgen. Kein Codeschritt nötig, wenn `s.photos.length <= 3` (typische Anzahl passt in Viewport) — **nur prüfen**, nicht ändern, wenn beim manuellen Test (Step 4) kein Problem auftritt.

- [ ] **Step 4: Manueller Mobile-Check**

Browser-DevTools auf iPhone-SE-Breite (375px) stellen, `/checkin/<ein-echter-token>` öffnen (Token aus der lokalen Dev-DB, z.B. über `api.clients.get` im Coach-Login nachschauen), durchklicken:
- Formular komplett sichtbar/ausfüllbar ohne horizontales Scrollen
- Datei-Auswahl-Button gut treffbar
- Submit-Button erreichbar ohne langes Scrollen
- Bestehende Check-in-Historie darunter lesbar, Fotos scrollbar

Gefundene zusätzliche Probleme hier direkt im selben Task beheben (kleinere Layout-Fixes, kein neuer Task nötig) und im Commit erwähnen.

- [ ] **Step 5: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/CheckinSubmit.tsx
git commit -m "fix: mobile usability improvements in public check-in form"
```

---

### Task 9: Dashboard-Priorisierung (visuelle Reihenfolge)

**Files:**
- Modify: `frontend/src/pages/Dashboard.tsx`

- [ ] **Step 1: Near-Limit-Banner direkt unter PageHeader belassen, aber optische Trennung zur Klientenliste schaffen**

Der Near-Limit-Banner steht bereits an der richtigen Stelle (direkt nach `PageHeader`, vor dem Formular/der Liste) — das ist strukturell schon korrekt (Trial-Banner lebt separat im `AppShell` darüber). Die eigentliche Lücke: zwischen Banner und dem Rest fehlt Abstand, wodurch es gedrängt wirkt. Ergänzen: nach dem Banner-Block einen Abstands-Wrapper einziehen. Ändern:

```tsx
      {user?.account_type === "coach" &&
        !["trialing", "active"].includes(user.subscription_status ?? "") &&
        clients.length >= 1 && (
          <UpgradeBanner message="Du nutzt bereits einen Klienten kostenlos — für weitere brauchst du ein Abo." />
        )}
```

zu (Banner bekommt eigenen Abstand nach unten, damit er sich klar vom Rest absetzt statt direkt ins Formular/die Liste überzugehen):

```tsx
      {user?.account_type === "coach" &&
        !["trialing", "active"].includes(user.subscription_status ?? "") &&
        clients.length >= 1 && (
          <div className="mb-4">
            <UpgradeBanner message="Du nutzt bereits einen Klienten kostenlos — für weitere brauchst du ein Abo." />
          </div>
        )}
```

- [ ] **Step 2: Typecheck**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 3: Manuell prüfen**

Als Coach-Testaccount ohne aktives Abo mit 1 Klient einloggen, Dashboard öffnen: Reihenfolge von oben nach unten ist PageHeader → Near-Limit-Banner (mit Abstand) → Formular (falls offen) → Suchleiste → Klientenliste/EmptyState. Trial-Banner (falls `trialing`-Status) erscheint im `AppShell` oberhalb des gesamten Seiteninhalts.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/Dashboard.tsx
git commit -m "fix: add visual spacing for dashboard banner hierarchy"
```

---

### Task 10: Finaler Review + Branch abschließen

- [ ] **Step 1: Vollständigen Typecheck laufen lassen**

Run: `cd frontend && npx tsc --noEmit`
Expected: keine Fehler

- [ ] **Step 2: Backend-Testsuite zur Sicherheit laufen lassen** (reine Frontend-Änderungen, aber Standard-Check vor Merge)

Run: `cd backend && .venv/Scripts/python -m pytest -q`
Expected: alle Tests grün (bis auf den bekannten, unabhängigen `test_gemini_key_is_scoped_per_account`-Fall bei lokal gesetztem `.env`-Key)

- [ ] **Step 3: Manuelle Gesamt-Durchsicht**

Alle geänderten Seiten einmal durchklicken (Dashboard leer + mit Klienten, Account, Timeline leer + mit Fotos, Unprocessed leer + mit Fotos, ClientCheckins leer, CheckinSubmit auf Mobile-Breite).

- [ ] **Step 4: `superpowers:finishing-a-development-branch` nutzen**

Tests verifizieren (bereits in Step 1/2 geschehen), Merge nach `dev` anbieten, danach `dev`→`main` nur nach expliziter Nutzer-Bestätigung.
